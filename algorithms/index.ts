export const arr = [9, 3, 4, 1, 5, 8, 2, 7, 6, 0]

export const comparator = (a: number, b: number) => a - b

export const swap = (arr: Array<number>, i: number, j: number) => [arr[i], arr[j]] = [arr[j], arr[i]]
const arr = [500, 501, 300]

const restaurant = (arr) => {
    let dp = Array.from({length: arr.length}, () => 0)
    let sum = 0
    let coupons

    dp[0] = 1

    for(let i = 0; i < arr.length; i++) {
        
    }

}